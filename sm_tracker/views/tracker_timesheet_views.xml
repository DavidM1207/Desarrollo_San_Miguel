<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        
        <record id="view_tracker_timesheet_tree" model="ir.ui.view">
            <field name="name">tracker.timesheet.tree</field>
            <field name="model">tracker.timesheet</field>
            <field name="arch" type="xml">
                <tree string="Registros de Tiempo" decoration-success="state=='stopped'" decoration-warning="state=='running'">
                    <field name="date"/>
                    <field name="task_id"/>
                    <field name="employee_id"/>
                    <field name="start_time" widget="datetime"/>
                    <field name="end_time" widget="datetime"/>
                    <field name="hours" widget="float_time" sum="total_hours"/>
                    <field name="state" widget="badge"/>
                </tree>
            </field>
        </record>

        <record id="view_tracker_timesheet_form" model="ir.ui.view">
            <field name="name">tracker.timesheet.form</field>
            <field name="model">tracker.timesheet</field>
            <field name="arch" type="xml">
                <form string="Registro de Tiempo">
                    <sheet>
                        <group>
                            <group>
                                <field name="task_id" options="{'no_create': True}"/>
                                <field name="project_id" readonly="1"/>
                                <field name="employee_id" options="{'no_create': True}"/>
                                <field name="user_id"/>
                            </group>
                            <group>
                                <field name="analytic_account_id" options="{'no_create': True}"/>
                                <field name="date"/>
                                <field name="state"/>
                            </group>
                        </group>
                        <group>
                            <group>
                                <field name="start_time" widget="datetime"/>
                                <field name="end_time" widget="datetime"/>
                            </group>
                            <group>
                                <field name="hours" widget="float_time"/>
                            </group>
                        </group>
                        <group>
                            <field name="name" placeholder="Descripción del trabajo realizado..."/>
                        </group>
                        <group>
                            <field name="notes" placeholder="Notas adicionales..."/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="view_tracker_timesheet_search" model="ir.ui.view">
            <field name="name">tracker.timesheet.search</field>
            <field name="model">tracker.timesheet</field>
            <field name="arch" type="xml">
                <search string="Buscar Registros">
                    <field name="task_id"/>
                    <field name="employee_id"/>
                    <field name="date"/>
                    <separator/>
                    <filter string="Mis Registros" name="my_timesheets" domain="[('employee_id.user_id','=',uid)]"/>
                    <separator/>
                    <filter string="En Progreso" name="running" domain="[('state','=','running')]"/>
                    <filter string="Detenidos" name="stopped" domain="[('state','=','stopped')]"/>
                    <separator/>
                    <filter string="Hoy" name="today" domain="[('date','=',context_today().strftime('%Y-%m-%d'))]"/>
                    <filter string="Esta Semana" name="this_week" domain="[('date','&gt;=', (context_today() - datetime.timedelta(days=context_today().weekday())).strftime('%Y-%m-%d'))]"/>
                    <separator/>
                    <filter string="Tarea" name="group_task" context="{'group_by':'task_id'}"/>
                    <filter string="Empleado" name="group_employee" context="{'group_by':'employee_id'}"/>
                    <filter string="Fecha" name="group_date" context="{'group_by':'date'}"/>
                </search>
            </field>
        </record>

        <record id="action_tracker_timesheet" model="ir.actions.act_window">
            <field name="name">Registros de Tiempo</field>
            <field name="res_model">tracker.timesheet</field>
            <field name="view_mode">tree,form</field>
            <field name="search_view_id" ref="view_tracker_timesheet_search"/>
            <field name="context">{'search_default_my_timesheets': 1, 'search_default_this_week': 1}</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    No hay registros de tiempo
                </p>
                <p>
                    Los registros se crean automáticamente cuando se inician las tareas.
                </p>
            </field>
        </record>

    </data>
</odoo>